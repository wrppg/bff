name: Test - Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v4
      - run: |
          gh auth login --with-token <<<"${{ github.token }}"
          gh workflow run LS.yaml
